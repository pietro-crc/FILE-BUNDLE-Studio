# Changelog

All notable changes follow Keep a Changelog conventions. The project is not yet released.

## [Unreleased]

### Added

- STEP-009 bounded browser-local secret scanner with sensitive-name, private-key, selected cloud/token, JWT, connection-string, password-assignment, and optional high-entropy detection.
- Report-only, derived-text redaction, and complete derived-output exclusion policies that never mutate original bytes or store matched values in Manifest v1.
- Security summaries, per-file category/count evidence, visual omission, exclusion-total recomputation, and cross-output validators rejecting stale Markdown/PDF references.
- Privacy-preserving React error boundary, harmless defensive fixtures, security benchmark, and prepared compiled Chromium flow.
- No new dependency; official npm/Vite/Vitest/Playwright/audit rerun remains required because the internal registry returned HTTP 503 during exact lockfile restoration in this environment.

- STEP-008 production DOCX/DOCM semantic extraction through the pinned Mammoth browser build with external file access disabled, bounded OOXML/media inspection, complete-read hashing, and mandatory fidelity warnings.
- Strict local Office HTML allowlist sanitizer that rebuilds inert semantic markup, strips active elements/attributes/links/remote resources, and never injects Mammoth output into the application DOM.
- DOCX headings, paragraphs, lists, tables, metadata, inert links, supported images, parser messages, macro flags, external relationship counts, Markdown, and derived visual pages.
- Production PPTX/PPTM PresentationML fallback preserving slide order, text, speaker notes, tables, safe media inventory, metadata, and simplified one-page-per-slide PDF rendering.
- Macro, chart, embedded-object, audio/video, and external-relationship inventory without execution, loading, playback, or fidelity claims.
- Office Manifest/Documents mappings, bounded UI metrics, 72 regression tests, six benchmarks, eight compiled Chromium E2E flows, screenshots, and a DOCM/PPTM benchmark.
- No new dependency; Mammoth, fflate, pdf-lib, DOMParser, and local allowlist code remain browser-local and audit-clean.

- STEP-007 production PDF adapter using local PDF.js byte parsing, per-page text/dimensions/rotation, JavaScript inventory, limits, cancellation, and explicit password-protection failure without bypass.
- Original PDF page copying through lazy `pdf-lib`, plus cover, instructions, index, separators, error pages, final report, strict global page budget, and source/output page mapping.
- Production PNG/JPEG/GIF/WebP/BMP/TIFF header inspection, byte/dimension/megapixel defenses before decode, EXIF/TIFF orientation handling, animation warnings, direct PNG/JPEG embedding, and bounded browser-native PNG derivation.
- Manifest v1 visual representation enrichment, cross-validation, PDF/image workflow UI, seven compiled Chromium E2E flows, harmless encrypted/oversized fixtures, screenshots, and a 25-page/eight-image benchmark.
- No new dependency; existing PDF.js, pdf-lib, browser image APIs, and local header readers remain bundled/local-only.

- STEP-006 defensive XLSX/XLSM OOXML adapter with bounded package/XML reads and per-file failure isolation.
- Workbook extraction for sheet visibility/order, ranges, values, cached formulas, dates, comments, merges, hidden rows/columns, defined names, and unsupported feature flags.
- Formula non-evaluation, DTD/entity rejection, external-relationship blocking, macro inventory, and formula-like literal protection in derived output.
- Bounded workbook Markdown, lazy multipage PDF sheet preview, manifest enrichment, compiled XLSX E2E coverage, screenshots, and a 2,000-cell benchmark.
- XLS and ODS explicitly degraded to inventory level D rather than receiving an untested conversion claim.

- STEP-005 production text/code adapter with bounded lazy reads, cancellation, and per-file error isolation.
- UTF-8/BOM/UTF-16 decoding, controlled Windows-1252 fallback, LF normalization, optional line numbers, and explicit truncation metadata.
- Deterministic inert Markdown anchors, collision-safe fences, UTF-8 byte-aware multipart sharding, and AI-oriented package instructions.
- Manifest v1 enrichment with adapter/version, complete-read SHA-256, encoding, anchors, parts, conversion status, and sharding references.
- Markdown/manifest cross-validation, bounded React preview, complete compiled workflow E2E, responsive screenshots, and a 500-file benchmark.
- Isolated Vitest and Playwright JSON-report runners that preserve all assertions while terminating only lingering teardown workers after verified success.

- STEP-004 versioned Manifest v1 JSON Schema, typed model, generator, canonical serializer, and browser-side consistency validator.
- Stable SHA-256 file and directory IDs independent of acquisition order and sequence-based VFS IDs.
- Explicit inclusion reasons, pending integrity fields, planned output references, authoritative tree, and cross-consistent summaries.
- Manifest builder UI with bounded JSON preview, compiled E2E flow, golden tests, mutation tests, and 1,000-file benchmark.
- Controlled Playwright runner with a global timeout and one retry only for infrastructure hangs; assertion failures are never retried.

- STEP-003 bounded preflight classification from signatures, local name/extension registry, declared MIME, and text/binary heuristics.
- Capability levels A–E, adapter ownership, support reasons, and conservative executable/binary-spoof handling.
- Risk signals for active content, macros, executables, nested archives, sensitive names, MIME mismatch, size, compression ratio, and decoding ambiguity.
- Source/logical size separation, output and working-memory estimate ranges, and advisory mode recommendations.
- Cancellable preflight UI with progress, search, capability/risk filters, manual inclusion, and bounded glob exclusions.
- Incremental ZIP entry-prefix reads, synthetic 1,000-file benchmark, responsive screenshots, and expanded unit/integration/E2E coverage.

- STEP-002 file, directory, drag-and-drop, and ZIP acquisition, including empty directories when the browser API or archive exposes them.
- Lazy disposable byte sources and metadata-only React VFS snapshots.
- NFC/separator path normalization with traversal, absolute-path, duplicate, control-character, depth, length, and hierarchy-conflict defenses.
- ZIP central-directory validation, encryption/method checks, resource limits, and lazy single-entry reads.
- Normalized tree, acquisition summary, issue reporting, live status, and responsive import screenshots.
- Unit/integration coverage for path policy, VFS disposal, cancellation, archive limits, encryption, and collisions.

- STEP-001 responsive application shell with six workflow destinations.
- Reusable CSS-token design system and accessible UI primitives.
- System, light, and dark theme controls.
- Skip navigation, focus transfer, reduced-motion behavior, and narrow-layout E2E coverage.
- Compiled desktop/mobile visual validation screenshots.

- STEP-000 Vite, React, and strict TypeScript baseline.
- Local-only privacy policy and static GitHub Pages configuration.
- Architecture contracts for the virtual filesystem, adapters, and observable pipeline.
- Disposable browser feasibility probes for ZIP, PDF, DOCX, and XLSX.
- Unit, integration, browser E2E, audit, and benchmark baselines.
- Initial product, architecture, security, dependency, test, support, and roadmap documentation.
